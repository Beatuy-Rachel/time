# 镜像配置
IMAGE=ghcr.io/beatuy-rachel/time
IMAGE_TAG=latest

# 服务器对外端口，容器内部固定使用 80 端口
HOST_PORT=8080

# 健康检查最长等待时间（秒）
DEPLOY_WAIT_TIMEOUT=120

# 如果 GHCR 镜像是私有的，取消注释并填写。
# 不要把真实 Token 提交到 GitHub。
# GHCR_USERNAME=Beatuy-Rachel
# GHCR_TOKEN=your-read-packages-token
